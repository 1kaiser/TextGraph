import { createApp } from 'vue';
import App from './App.vue';
import '../style.css'; // Keep original styles for now
import '../text-as-graph.css'; // Keep original styles for now

createApp(App).mount('#app');
